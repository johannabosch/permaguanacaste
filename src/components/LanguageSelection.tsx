'use client';

import Hero from './Hero';

const LanguageSelection = () => {
  return (
    <main className="min-h-screen">
      <Hero isLanguageSelection={true} />
    </main>
  );
};

export default LanguageSelection;
